Face Recognition using HaarCascadeFrontalFace Algorithm.
This model will display the name of the person before camera.(Same as the name of dataset and dataset should be present in the directory).

The model's accuracy will depend on the dataset created or uploaded using faceDatasetCreation.py which will take 30 frames for training.